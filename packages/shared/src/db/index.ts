/**
 * Database Schema Placeholder
 *
 * This file will contain the Drizzle ORM schema definitions.
 * Currently a placeholder - will be populated when setting up the database.
 *
 * The schema will define tables for:
 * - users: User accounts (via NextAuth)
 * - resumes: Uploaded and parsed resumes
 * - jobs: Scraped job postings
 * - companies: Watched companies
 * - job_matches: AI matching results
 * - user_preferences: Job search preferences
 */

// Placeholder export to prevent empty module error
export const DB_VERSION = '0.1.0';

// TODO: Add Drizzle schema when database is set up
// export * from './schema';
// export * from './client';
